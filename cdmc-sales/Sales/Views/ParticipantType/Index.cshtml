@using Entity
@(Html.Telerik().Grid<ParticipantType>().Name("ParticipantType")
                .DataKeys(keys =>
                {
                    keys.Add(s => s.ID);
                })
                .Columns(c =>
                {
                    c.Bound(item => item.Name).Width(200);
                    c.Bound(item => item.Description).Width(300);
                    c.Bound(item => item.ID).Title("操作").Template(@<span> <a href=@Url.Content("~/ParticipantType/Edit/" + item.ID) >
                        编辑</a> | <a href=@Url.Content("~/ParticipantType/Details/" + item.ID)>详细</a> | <a href=@Url.Content("~/ParticipantType/delete/" + item.ID) >
                            删除</a> </span>);
                })
                                            .Resizable(resizing => resizing.Columns(true))
                                            .Filterable().Sortable().Groupable()
                                            .Pageable(p => p.PageSize(20))
                                            .Scrollable(scrolling => scrolling.Height(350))

                            )
@Html.ActionLink("创建", "Create")