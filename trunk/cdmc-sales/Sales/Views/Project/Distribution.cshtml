@{
    ViewBag.Title = "Distribution";
}
@using Entity
@using Utl
@model List<Member>
@Html.Partial("contenttitle", "字头分配")
@helper CompanysTree()
    {
    <div class="zimu_left">
        <h2>
            备选字母</h2>
        <ul class="charatacter connectedSortable">
            @if (ViewBag.DC != null)
            {
                var dc = (List<string>)ViewBag.DC;
                foreach (var c in dc)
                {
                <li>@c</li> 
                }
            }
        </ul>
    </div>
}
@helper Members()
    {
        var Members = CH.GetAllData<Member>(i => i.ProjectID == (int)ViewBag.ProjectID);
    <div class="zimu_right">
        @if (Members != null)
        {
            foreach (var m in Members)
            {
            <div>
                <h3>@m.Name:</h3>
                <ul class="drop-container connectedSortable pane" mid=@m.ID>
                    @foreach (var c in m.CharactersSet)
                    {
               
                        <li>@c<input name="mc" type="hidden" value=@m.ID|@c />
                        </li> 
                    }
                </ul>
            </div>
            }
        }
    </div>
        
           

       

   
}
<div class="xiangmu_2">
    <div class="yuanjiao">
    </div>
    <div class="xiangmu_3">
     @{ Html.BeginForm(); }
        @CompanysTree()
        @Members()
       
        @Html.Hidden("ProjectID", (int)ViewBag.ProjectID)
       
      @*  <div class="anniu_2">
            <ul>
                <li>
               
                  </li>
            </ul>
        </div>*@
         <input type="submit"  value="提交" style="margin-top:450px; margin-left:-600px;"  />
        @{ Html.EndForm(); }
    </div>
     
    <div class="yuanjiao_2">
    </div>
</div>
<style type="text/css" type="text/css">
    #TreeView
    {
        height: 24em;
        padding: .5em;
    }
    .drop-container
    {
        height: 8em;
        overflow: auto;
        margin-left: 15px;
        border-width: 1px;
        border-style: solid;
        width: 93%;
        padding-top: 15px;
    }
    .charatacter
    {
         max-width:200px; min-height:350px;
    }
    .charatacter li, .pane li
    {
        float: left;
        border-width: 1px;
        border-style: solid;
        list-style-type: none;
        min-width: 50px;
        text-align: center;
        margin-left: 5px;
    }
    h3
    {
        font-family: "Trebuchet MS" , Arial, Helvetica, sans-serif;
        color: #d96a10;
        font-size: 14px;
    }
</style>
@{ Html.Telerik().ScriptRegistrar().OnDocumentReady(@<text> initialCharDistribution()
    </text>);
}
