@using Entity
@model List<TemplateType>
@Html.Partial("contenttitle", "模版列表")
@(Html.Telerik().Grid<TemplateType>(Model).Name("TemplateType").ToolBar(commands => commands.Custom().Text("模板类型创建").Action("Create", "TemplateType"))
                .DataKeys(keys =>
                {
                    keys.Add(s => s.ID);
                })
                .Columns(c =>
                {
                    c.Bound(item => item.Name);
                    c.Bound(item => item.ID).Width(200).Title("操作").ClientTemplate("<a href='/TemplateType/edit/" + "<#= ID #>'>编辑</a> | <a href='/TemplateType/details/" + "<#= ID #>'>详细</a> | <a href='/TemplateType/delete/" + "<#= ID #>'>删除</a>");
                })
                .Resizable(resizing => resizing.Columns(true))
                .Filterable().Sortable().Groupable()
                .Pageable(p => p.PageSize(20))
                .Scrollable(scrolling => scrolling.Height(350))

)
