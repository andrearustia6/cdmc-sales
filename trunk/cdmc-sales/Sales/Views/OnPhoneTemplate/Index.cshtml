@using Entity
@model List<OnPhoneTemplate>
@Html.Partial("contenttitle", "话术模版")
@(Html.Telerik().Grid<OnPhoneTemplate>(Model).Name("OnPhoneTemplate")
                .DataKeys(keys =>
                {
                    keys.Add(s => s.ID);
                })
                .Columns(c =>
                {
                    c.Bound(item => item.Name).Width(200);
                    c.Bound(item => item.OnPhoneBlockType.Name).Width(200);
                    c.Bound(item => item.Block).Width(200);
                    c.Bound(item => item.Answer);
                    c.Bound(item => item.ID).Title("操作").Width(200).Template(@<span> <a href=@Url.Content("~/OnPhoneTemplate/Edit/" + item.ID) >
                        编辑</a> | <a href=@Url.Content("~/OnPhoneTemplate/Details/" + item.ID)>详细</a> | <a href=@Url.Content("~/OnPhoneTemplate/delete/" + item.ID) >
                            删除</a> </span>);
                })
                                            .Resizable(resizing => resizing.Columns(true))
                                            .Filterable().Sortable().Groupable()
                                            .Pageable(p => p.PageSize(20))
                                            .Scrollable(scrolling => scrolling.Height(350))

                            )
<div class="anniu">
    <ul>
        <li>@Html.ActionLink("话术模版创建", "Create")</li>
    </ul>
</div>
